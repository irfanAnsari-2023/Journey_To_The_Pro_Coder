const JSXRules = ()=> {
    return (
    <div>
        <h1>JSX Rules</h1>
        <p>Three rules of JSX</p>
         <ul>
                <li>JSX must return a single parent element.</li>
                <li>JSX elements must be properly closed.</li>
                <li>JSX attr. are written using camelCase(eg. className, htmlFor etc.)</li>
            </ul>
    </div>
    )
}

export default JSXRules;